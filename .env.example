# Required
ANTHROPIC_API_KEY=sk-ant-...

# Daily.co — either provide DAILY_ROOM_URL (use an existing room)
# or DAILY_API_KEY (the agent will create a temporary room on startup)
DAILY_API_KEY=
DAILY_ROOM_URL=

# Optional: override locale for after-hours messages ("de" or "us")
OFFICE_LOCALE=de
